datasource db {
  provider = "sqlite"
  url      = "file:db/dev.db"
  default  = true
}

model User {
  id        Int @id
  username  String @unique
  name      String?
  isActive  Boolean @default(true)
  lastLogin DateTime
}
