# Migration `20190617211558-initial-model`

This migration has been generated by Peter Pistorius at 6/17/2019, 9:15:58 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "dev"."User" ("id" INTEGER DEFAULT 0 NOT NULL, "username" TEXT DEFAULT '' NOT NULL, "name" TEXT, "isActive" BOOLEAN DEFAULT true NOT NULL, PRIMARY KEY ("id"));

CREATE TABLE "dev"."Document" ("id" INTEGER DEFAULT 0 NOT NULL, "name" TEXT DEFAULT '' NOT NULL, "path" TEXT DEFAULT '' NOT NULL, "content" TEXT, "user" INTEGER REFERENCES User(id) NOT NULL, PRIMARY KEY ("id"));

CREATE TABLE "dev"."Comment" ("id" INTEGER DEFAULT 0 NOT NULL, "comment" TEXT DEFAULT '' NOT NULL, "createdAt" DATE DEFAULT '1970-01-01 00:00:00' NOT NULL, "updatedAt" DATE DEFAULT '1970-01-01 00:00:00' NOT NULL, "user" INTEGER REFERENCES User(id) NOT NULL, "document" INTEGER REFERENCES Document(id), PRIMARY KEY ("id"));
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190617211558-initial-model
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,33 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:db/dev.db"
+  default  = true
+}
+
+generator photon {
+  provider = "javascript"
+}
+
+model User {
+  id        Int       @id
+  username  String    @unique
+  name      String?
+  isActive  Boolean   @default(true)
+}
+
+model Document {
+  id        Int       @id
+  name      String
+  path      String    @unique
+  content   String?
+  user      User
+  comments  Comment[]
+}
+
+model Comment {
+  id        Int       @id
+  user      User
+  comment   String
+  createdAt DateTime
+  updatedAt DateTime
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190617211558-initial-model)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190617211558-initial-model'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
