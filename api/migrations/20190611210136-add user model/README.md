# Migration `20190611210136-add user model`

This migration has been generated by Peter Pistorius at 6/11/2019, 9:01:36 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "migration_engine"."User" ("id" INTEGER NOT NULL, "username" TEXT NOT NULL, "name" TEXT, "isActive" BOOLEAN NOT NULL, "lastLogin" DATE NOT NULL, PRIMARY KEY ("id"));
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration last migration id..20190611210136-add user model
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,7 @@
+model User {
+    id: Int @id
+    username: String @unique
+    name: String?
+    isActive: Boolean @default(true)
+    lastLogin: DateTime
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190611210136-add user model)
in your `before` or `after` migration script like this:

```ts
import Photon from "@generated/photon/20190611210136-add user model";

const photon = new Photon();

async function main() {
  const result = await photon.users();
  console.dir(result, { depth: null });
}

main();
```
